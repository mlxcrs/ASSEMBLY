	ORG		0H
	ljmp	100H
	ORG		100H
	MOV		P1,#00H
	MOV		P0,#00H
	MOV		A,#01H
;
	JMP 	ESQ
ANE:MOV		PSW,#00H
	MOV		A,R0
	MOV		P0,A
;
ESQ:RLC		A
;
	MOV		R0,A
	MOV		R1,PSW
	MOV		R2,P1
	SUBB	A,R2
	MOV		R3,A
	JZ		AND
	MOV		A,R0
	MOV		PSW,R1
;
	MOV		P0,A
	JNC		ESQ
;
	JMP 	DIR
AND:MOV		PSW,#00H
	MOV		A,R0
	MOV		P0,A
;
DIR:RRC		A
;
	MOV		R0,A
	MOV		R1,PSW
	MOV		R2,P1
	SUBB	A,R2
	MOV		R3,A
	JZ		ANE
	MOV		A,R0
	MOV		PSW,R1
;
	MOV		P0,A
	JNC		DIR
	SJMP	ESQ
ALT:
	END